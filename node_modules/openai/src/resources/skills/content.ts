// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.

import { APIResource } from 'openai/src/core/resource';
import { APIPromise } from 'openai/src/core/api-promise';
import { buildHeaders } from 'openai/src/internal/headers';
import { RequestOptions } from 'openai/src/internal/request-options';
import { path } from 'openai/src/internal/utils/path';

export class Content extends APIResource {
  /**
   * Download a skill zip bundle by its ID.
   */
  retrieve(skillID: string, options?: RequestOptions): APIPromise<Response> {
    return this._client.get(path`/skills/${skillID}/content`, {
      ...options,
      headers: buildHeaders([{ Accept: 'application/binary' }, options?.headers]),
      __binaryResponse: true,
    });
  }
}
